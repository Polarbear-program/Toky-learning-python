# an example of using range

# if we have a range of 6, defined with x
#x = range(6)
"""for n in x:
        print(n)"""

# The syntax here is range(stop), start default is 0, stop is 6, step/increments by 1(default)
# We can try to make something different

y = range(0, 20, 2)
for m in y:
    print(m)